const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const cargas = await prisma.horarioCargaDocente.findMany({ include: { grupo: true, asignatura: true, personal: true } }); console.log(cargas.filter(c => c.asignatura.uacName.includes('Educaci')).map(c => ({grupo: c.grupo.nombre, uac: c.asignatura.uacName, personal: c.personal.nombre}))); } main().finally(() => process.exit(0));
